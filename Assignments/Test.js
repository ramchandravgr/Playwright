// Initialize an empty array
let myArray = [];

// Initialize a counter
let i = 1;

// Use a while loop to add values to the array
while (true) {
    myArray.push("Value " + i);
    console.log("Added: Value " + i);
    i++;
}

// Display the final array
console.log("Final Array:", myArray);
